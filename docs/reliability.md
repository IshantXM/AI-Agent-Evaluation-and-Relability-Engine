# Reliability

Reliability assessment combines evaluator consensus, adversarial summary, and
evaluation coverage into a status and score. The report builder then records
per-dimension scores, findings, root causes, recommendations, and version
regression context.

A report is evidence for engineering review, not a guarantee that an agent is
safe or correct in all environments. Missing coverage and evaluator errors are
represented explicitly.
